# RunPod handler
runpod

# Upgrade PyTorch to 2.5+ (required for diffusers enable_gqa parameter)
# Base image has 2.4.0 but diffusers needs 2.5+
torch>=2.5.0
torchvision>=0.20.0
torchaudio>=2.5.0

# Diffusers from GitHub (ZImagePipeline not in stable release yet)
git+https://github.com/huggingface/diffusers

# Required for Z-Image text encoder
transformers
sentencepiece
protobuf

# Other dependencies
accelerate
pillow
huggingface-hub
